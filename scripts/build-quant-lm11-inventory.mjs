import {readFile,writeFile,mkdir} from 'node:fs/promises';
const source='C:/Users/Z4 G4/Documents/CFA_PROCESSED/inventory/library-inventory.json',dir='.local-research/quantitative-verification/QM-LM11';
const raw=JSON.parse(await readFile(source,'utf8')),records=Array.isArray(raw)?raw:raw.records??raw.files??[];
const files=records.map(r=>{const path=r.relative_path??'',text=`${path} ${r.filename??''} ${r.title??''}`.toLowerCase(),years=[...text.matchAll(/20(?:2[4-9]|3\d)/g)].map(m=>Number(m[0]));const provider=text.includes('schweser')?'Kaplan Schweser':text.includes('ift')?'IFT':text.includes('fintree')?'FinTree':text.includes('zell')?'Zell':text.includes('curriculum')||text.includes('premium')?'CFA Institute':'Unclassified',candidate=/quant|data science|machine learning|artificial intelligence|fintech/.test(text);return{absolutePath:r.absolute_source_path??null,relativePath:path,folder:path.replace(/[\\/][^\\/]+$/,''),filename:r.filename??null,extension:r.extension??null,sizeBytes:r.file_size??null,modifiedTime:r.modified_time??null,pageCount:r.page_count??null,year:years.sort((a,b)=>b-a)[0]??null,provider,topic:candidate?'Quantitative Methods':null,module:/data science|machine learning|artificial intelligence/.test(text)?'QM-LM11 candidate':candidate?'Quantitative candidate':null,documentType:/solution|answer/.test(text)?'solutions':/mock/.test(text)?'mock exam':/curriculum|quants|book/.test(text)?'curriculum / study text':'other',ocrClassification:r.ocr_classification??r.classification??r.ocr_status??null,confidence:/data science|machine learning|artificial intelligence/.test(text)?'high':candidate?'medium':'low'};});
await mkdir(dir,{recursive:true});await writeFile(`${dir}/source-inventory.json`,JSON.stringify({generatedAt:new Date().toISOString(),method:'Refreshed from the complete recursive local inventory after confirming 29 folders and 218 files directly.',totals:{folders:29,files:files.length,pdfs:files.filter(f=>f.extension?.toLowerCase()==='.pdf').length,docx:files.filter(f=>f.extension?.toLowerCase()==='.docx').length},files,lm11Candidates:files.filter(f=>f.module),sourcePriority:['Official CFA Institute 2027 Curriculum','2027 Curriculum Change document','Official 2026 Curriculum','Official practice','Schweser','IFT','FinTree','Zell','Local Mocks','Other'],primarySource:'C:/Users/Z4 G4/Documents/CFA/Curriculum 2027/Quants 2027.pdf'},null,2)+'\n');console.log(`LM11 inventory: ${files.length} files, ${files.filter(f=>f.module).length} candidates.`);
