export function renderSidebar(c,p,onOpen){const el=document.querySelector("#sidebarContent");el.innerHTML=c.topics.map(t=>`<section class="topic-block"><button class="topic-title">${t.title}</button><div>${t.lessons.map(l=>`<button class="lesson-link ${p.completedLessons.includes(l.id)?"done":""}" data-id="${l.id}"><span>${p.completedLessons.includes(l.id)?"✓":"○"}</span><span>${l.title}</span></button>`).join("")||'<span class="coming-soon">Content coming soon</span>'}</div></section>`).join("");el.querySelectorAll(".lesson-link").forEach(b=>b.onclick=()=>onOpen(b.dataset.id))}