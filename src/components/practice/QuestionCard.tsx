import type {Question} from '../../data/types'; export function QuestionCard({question,selected,onSelect,revealed}:{question:Question;selected:number|null;onSelect:(i:number)=>void;revealed:boolean}){return <div><div className="mb-4 flex justify-between text-sm text-slate-500"><span className="capitalize">{question.difficulty}</span><span>~{question.estimatedSeconds} sec</span></div><h2 className="text-xl">{question.stem}</h2><div className="mt-5 space-y-3">{question.choices.map((choice,i)=>{const state=revealed?(i===question.correctChoiceIndex?'correct':i===selected?'incorrect':''):selected===i?'selected':'';return <button disabled={revealed} key={choice} onClick={()=>onSelect(i)} className={`choice ${state}`}><span>{String.fromCharCode(65+i)}</span>{choice}</button>})}</div></div>}
