
import {Link} from 'react-router-dom';import type {Question} from '../../data/types'; export function AnswerExplanation({question,selected}:{question:Question;selected:number}){return <div className="mt-5 rounded-lg border bg-slate-50 p-4 dark:border-slate-700 dark:bg-slate-900"><h3>{selected===question.correctChoiceIndex?'Correct':'Not quite'}</h3><p className="mt-2">{question.explanation}</p><ul className="mt-3 space-y-2 text-sm">{question.choices.map((c,i)=><li key={c}><strong>{String.fromCharCode(65+i)}.</strong> {question.incorrectChoiceExplanations[i]}</li>)}</ul><div className="mt-4 flex flex-wrap gap-2"><Link className="btn" to={`/lessons/${question.lessonId}`}>Review lesson</Link>{question.relatedFormulaIds.map(id=><Link className="btn" key={id} to={`/formulas#${id}`}>Review formula</Link>)}</div></div>}
