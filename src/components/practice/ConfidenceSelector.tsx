import type {Confidence} from '../../data/types'; export function ConfidenceSelector({value,onChange}:{value:Confidence|null;onChange:(v:Confidence)=>void}){return <fieldset className="mt-4"><legend className="text-sm font-semibold">How confident were you?</legend><div className="mt-2 flex gap-2">{(['confident','unsure','guess'] as const).map(x=><button type="button" key={x} onClick={()=>onChange(x)} className={`btn capitalize ${value===x?'btn-primary':''}`}>{x}</button>)}</div></fieldset>}
