import {useState} from 'react';const scenarios=[{prompt:'Slope is 0.8 and X rises by one unit.',choices:['Predicted Y rises 0.8','Y is caused to rise 0.8','80% of observations are correct'],answer:0,why:'The slope is the predicted change in Y per unit of X, not proof of causation.'},{prompt:'R² is 0.64.',choices:['64% of observations are correct','64% of sample Y variation is explained by the fitted linear model','Correlation proves causation'],answer:1,why:'R² is the fitted proportion of sample variation in Y explained by the regression.'},{prompt:'A slope p-value is 0.03 at alpha 0.05.',choices:['Fail to reject zero slope','Reject zero slope, then assess economic importance','The slope is economically large'],answer:1,why:'The slope is statistically significant at 5%, but effect size still requires practical assessment.'}];export function RegressionOutputInterpreter(){const[i,setI]=useState(0),[choice,setChoice]=useState<number|null>(null);const s=scenarios[i];return <section className="card"><h2>Regression output interpreter</h2><label className="label mt-3">Scenario<select value={i} onChange={e=>{setI(Number(e.target.value));setChoice(null)}}>{scenarios.map((_,j)=><option key={j} value={j}>Scenario {j+1}</option>)}</select></label><p className="mt-3">{s.prompt}</p><div className="mt-2 space-y-2">{s.choices.map((c,j)=><button className="block w-full rounded border p-2 text-left" key={c} onClick={()=>setChoice(j)}>{c}</button>)}</div>{choice!==null&&<p role="status" className="mt-3"><strong>{choice===s.answer?'Correct.':'Not quite.'}</strong> {s.why}</p>}</section>}
