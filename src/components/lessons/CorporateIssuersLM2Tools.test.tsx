import {fireEvent,render,screen} from '@testing-library/react';import {describe,expect,it} from 'vitest';import {StakeholderClaimPriorityExplorer,claimProfiles} from './StakeholderClaimPriorityExplorer';import {ESGRiskOpportunityClassifier,esgScenarios} from './ESGRiskOpportunityClassifier';
describe('Corporate Issuers LM2 tools',()=>{it('explains claim priority across deterministic cash-flow states',()=>{render(<StakeholderClaimPriorityExplorer/>);expect(screen.getByText(/Promised payments are well covered/)).toBeInTheDocument();fireEvent.change(screen.getByLabelText('Issuer cash-flow state'),{target:{value:'liquidation'}});expect(screen.getByText(/Common shareholders receive only the residual/)).toBeInTheDocument();expect(claimProfiles.liquidation.sequence).toHaveLength(4)});it('classifies issuer-specific ESG exposure with reasoning',()=>{render(<ESGRiskOpportunityClassifier/>);expect(screen.getByText('Physical risk')).toBeInTheDocument();fireEvent.change(screen.getByLabelText('ESG issuer scenario'),{target:{value:'supplier-labor'}});expect(screen.getByText('Value-chain risk')).toBeInTheDocument();expect(screen.getByText('Indirect')).toBeInTheDocument();expect(esgScenarios['supplier-labor'].category).toBe('Social')})});
