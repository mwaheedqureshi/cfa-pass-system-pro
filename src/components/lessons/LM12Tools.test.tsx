import {fireEvent,render,screen} from '@testing-library/react';import {describe,expect,it} from 'vitest';import {buildForecast,defaultForecastInputs,LinkedFinancialStatementForecastBuilder} from './LinkedFinancialStatementForecastBuilder';import {evaluateScenario,ForecastAssumptionScenarioExplorer} from './ForecastAssumptionScenarioExplorer';
describe('LM12 forecasting tools',()=>{it('links the default forecast and balances deterministically',()=>{const r=buildForecast(defaultForecastInputs);expect(r.revenue).toBe(1100);expect(r.netIncome).toBe(153.75);expect(r.endingPpe).toBe(630);expect(r.retainedEarnings).toBe(513.75);expect(r.balanceCheck).toBeCloseTo(0,8)});it('keeps linked statements balanced after an assumption change',()=>{render(<LinkedFinancialStatementForecastBuilder/>);expect(screen.getByText(/balanced$/)).toBeInTheDocument();fireEvent.change(screen.getByLabelText('Sales growth %'),{target:{value:'20'}});expect(screen.getByText(/balanced$/)).toBeInTheDocument();expect(screen.getByText('Revenue: 1200.0')).toBeInTheDocument()});it('compounds price and volume and applies cost inflation',()=>{const r=evaluateScenario({baseRevenue:1000,priceChange:2,volumeChange:3,costInflation:4,passThrough:50,baseGrossMargin:40,competitionPressure:1,horizon:5});expect(r.priceEffect).toBe(3);expect(r.revenue).toBeCloseTo(1060.9);expect(r.cost).toBeCloseTo(642.72)});it('switches to a coherent downside scenario',()=>{render(<ForecastAssumptionScenarioExplorer/>);fireEvent.click(screen.getByRole('button',{name:'downside'}));expect(screen.getByLabelText('Volume change %')).toHaveValue(-4);expect(screen.getByLabelText('Cost pass-through %')).toHaveValue(30)})});
