import {Bookmark,Clock,Gauge} from 'lucide-react'; import type {Lesson} from '../../data/types'; import {Button} from '../common/Button'; export function LessonHeader({lesson,bookmarked,onBookmark}:{lesson:Lesson;bookmarked:boolean;onBookmark:()=>void}){return <header className="mb-6"><p className="text-sm font-medium text-blue-600">Quantitative Methods / {lesson.officialModuleTitle}</p><div className="mt-2 flex items-start justify-between gap-4"><h1>{lesson.title}</h1><Button onClick={onBookmark} aria-pressed={bookmarked} aria-label={bookmarked?'Remove bookmark':'Bookmark lesson'}><Bookmark fill={bookmarked?'currentColor':'none'} size={18}/></Button></div><div className="mt-4 flex flex-wrap gap-3 text-sm text-slate-500"><span className="meta"><Clock size={16}/>{lesson.estimatedMinutes} min</span><span className="meta"><Gauge size={16}/>{lesson.difficulty}</span><span className="badge">Independent explanation</span></div></header>}
