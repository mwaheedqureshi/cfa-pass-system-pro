import {fireEvent,render,screen} from '@testing-library/react';import {describe,expect,it} from 'vitest';import {GovernanceConflictClassifier,governanceConflicts} from './GovernanceConflictClassifier';import {GovernanceRiskBenefitMatrix,governanceMechanisms} from './GovernanceRiskBenefitMatrix';describe('Corporate Issuers LM3 tools',()=>{it('classifies governance conflicts deterministically',()=>{render(<GovernanceConflictClassifier/>);expect(screen.getByText('Principal–agent')).toBeInTheDocument();fireEvent.change(screen.getByLabelText('Governance conflict scenario'),{target:{value:'controller-transfer'}});expect(screen.getByText('Principal–principal')).toBeInTheDocument();expect(governanceConflicts['controller-transfer'].signal).toContain('Self-dealing')});it('shows benefits, residual risks, and limitations',()=>{render(<GovernanceRiskBenefitMatrix/>);expect(screen.getByText(/Independence alone/)).toBeInTheDocument();fireEvent.change(screen.getByLabelText('Governance mechanism'),{target:{value:'covenant'}});expect(screen.getByText('Can block valuable flexibility')).toBeInTheDocument();expect(governanceMechanisms.covenant.residual).toContain('enforcement')})});
