import {useState} from 'react';const cases=[['A generated memo cites a nonexistent filing.','Hallucinated facts','Verify every claim against authoritative sources.'],['A prompt contains confidential client holdings.','Confidential-data exposure','Use approved systems and never submit restricted data.'],['The model summarizes market conditions using an old knowledge cutoff.','Stale information','Retrieve and verify current dated sources.'],['Minor prompt wording produces opposite recommendations.','Prompt sensitivity','Test robust prompts and require human review.'],['An analyst publishes output without checking citations.','Missing verification','Require source tracing and accountable approval.']];export function GenerativeAIRiskExplorer(){const[i,setI]=useState(0),[choice,setChoice]=useState('');return <section className="card"><h2>Generative AI risk explorer</h2><label className="label mt-3">Workflow scenario<select value={i} onChange={e=>{setI(Number(e.target.value));setChoice('')}}>{cases.map((_,j)=><option key={j} value={j}>Scenario {j+1}</option>)}</select></label><p className="mt-3">{cases[i][0]}</p><label className="label mt-3">Primary risk<select value={choice} onChange={e=>setChoice(e.target.value)}><option value="">Choose…</option>{cases.map(x=><option key={x[1]}>{x[1]}</option>)}</select></label>{choice&&<p role="status">{choice===cases[i][1]?'Correct. ':'Not the primary risk. '}<strong>Mitigation:</strong> {cases[i][2]}</p>}</section>}
