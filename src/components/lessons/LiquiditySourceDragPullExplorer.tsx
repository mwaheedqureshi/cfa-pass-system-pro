import{useState}from'react';
export const liquidityScenarios={operations:{label:'Routine operating cash flow',classification:'Primary source',effect:'Recurring core-business cash supports sustainable liquidity.'},assetSale:{label:'Distressed asset sale',classification:'Secondary source',effect:'Cash is raised at potentially substantial cost during stress.'},lateCustomer:{label:'Customer pays later',classification:'Liquidity drag',effect:'A cash inflow is delayed.'},earlySupplier:{label:'Supplier demands early payment',classification:'Liquidity pull',effect:'A cash outflow is accelerated.'},cancelledLine:{label:'Bank cancels credit line',classification:'Liquidity pull',effect:'Credit availability is interrupted.'}} as const;
export default function LiquiditySourceDragPullExplorer(){const[key,setKey]=useState<keyof typeof liquidityScenarios>('operations'),s=liquidityScenarios[key];return <section className="card p-5"><h3 className="text-lg font-semibold">Liquidity Source and Drag/Pull Explorer</h3><label className="mt-3 block text-sm">Scenario<select aria-label="Liquidity scenario" className="input mt-1" value={key} onChange={e=>setKey(e.target.value as keyof typeof liquidityScenarios)}>{Object.entries(liquidityScenarios).map(([k,v])=><option key={k} value={k}>{v.label}</option>)}</select></label><div className="mt-4 rounded-lg bg-slate-50 p-4"><strong>{s.classification}</strong><p className="mt-1 text-sm">{s.effect}</p></div></section>}
