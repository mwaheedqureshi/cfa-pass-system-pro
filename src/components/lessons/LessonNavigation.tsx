import {Link} from 'react-router-dom'; import type {Lesson} from '../../data/types'; import {Button} from '../common/Button'; export function LessonNavigation({complete,onComplete,previous,next}:{complete:boolean;onComplete:()=>void;previous?:Lesson;next?:Lesson}){return <div className="card mt-6"><div className="flex flex-wrap items-center justify-between gap-3"><div>{previous?<Link className="btn" to={`/lessons/${previous.id}`}>← {previous.title}</Link>:<span className="muted">First available lesson</span>}</div><div>{next?<Link className="btn" to={`/lessons/${next.id}`}>{next.title} →</Link>:<span className="muted">Latest available lesson</span>}</div></div><div className="mt-4 flex flex-wrap justify-end gap-2"><Link className="btn" to="/flashcards">Flashcards</Link><Link className="btn btn-primary" to="/practice">Start lesson quiz</Link><Button className={complete?'btn-success':'btn-primary'} onClick={onComplete}>{complete?'Completed ✓':'Mark complete'}</Button></div></div>}
