import {Link} from 'react-router-dom'; import {Button} from '../common/Button'; export function LessonNavigation({complete,onComplete}:{complete:boolean;onComplete:()=>void}){return <div className="card mt-6 flex flex-wrap items-center justify-between gap-3"><span className="muted">This is the first available lesson. More modules are content pending.</span><div className="flex gap-2"><Link className="btn" to="/flashcards">Flashcards</Link><Link className="btn btn-primary" to="/practice">Start lesson quiz</Link><Button className={complete?'btn-success':'btn-primary'} onClick={onComplete}>{complete?'Completed ✓':'Mark complete'}</Button></div></div>}
