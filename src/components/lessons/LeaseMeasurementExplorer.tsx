import{useMemo,useState}from'react';
export function presentValueOfLease(payment:number,rate:number,periods:number){let value=0;for(let t=1;t<=periods;t++)value+=payment/(1+rate)**t;return value}
export function leasePeriod(opening:number,payment:number,rate:number,model:'finance'|'us-operating',periods:number){const interest=opening*rate,principal=payment-interest,closing=Math.max(0,opening-principal),financeAmortization=opening/periods,rouAmortization=model==='finance'?financeAmortization:payment-interest;return{interest,principal,closing,rouAmortization,totalExpense:interest+rouAmortization,operatingCashFlow:model==='us-operating'?payment:interest,financingCashFlow:model==='us-operating'?0:principal}}
export function LeaseMeasurementExplorer(){const[payment,setPayment]=useState(100000),[rate,setRate]=useState(10),[periods,setPeriods]=useState(5),[model,setModel]=useState<'finance'|'us-operating'>('finance');const result=useMemo(()=>{const pv=presentValueOfLease(payment,rate/100,periods);return{pv,...leasePeriod(pv,payment,rate/100,model,periods)}},[payment,rate,periods,model]);return <div className="card"><h3>Lease measurement explorer</h3><p className="muted mt-1 text-sm">Measure the initial lease liability and compare finance/IFRS presentation with a US GAAP operating lease.</p><div className="mt-3 grid gap-3 sm:grid-cols-2"><label>Annual payment<input aria-label="Annual lease payment" type="number" value={payment} onChange={e=>setPayment(Number(e.target.value))}/></label><label>Discount rate (%)<input aria-label="Lease discount rate" type="number" value={rate} onChange={e=>setRate(Number(e.target.value))}/></label><label>Lease term<input aria-label="Lease term" type="number" min="1" value={periods} onChange={e=>setPeriods(Math.max(1,Number(e.target.value)))}/></label><label>Model<select aria-label="Lease accounting model" value={model} onChange={e=>setModel(e.target.value as typeof model)}><option value="finance">IFRS / finance lease</option><option value="us-operating">US GAAP operating lease</option></select></label></div><div className="mt-3" role="status"><p><strong>Initial liability and ROU asset:</strong> {result.pv.toFixed(0)}</p><p><strong>First-period interest:</strong> {result.interest.toFixed(0)}</p><p><strong>Principal:</strong> {result.principal.toFixed(0)}</p><p><strong>ROU amortization:</strong> {result.rouAmortization.toFixed(0)}</p><p><strong>Total expense:</strong> {result.totalExpense.toFixed(0)}</p><p><strong>CFO/financing cash outflow:</strong> {result.operatingCashFlow.toFixed(0)} / {result.financingCashFlow.toFixed(0)}</p></div></div>}
