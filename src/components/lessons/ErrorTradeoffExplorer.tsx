import {useState} from 'react';export function ErrorTradeoffExplorer(){const[alpha,setAlpha]=useState(.05),[sample,setSample]=useState(50),[effect,setEffect]=useState(1);const illustrative=Math.min(.99,Math.max(.01,.35+Math.sqrt(sample)/25+effect/8-alpha));return <section className="card"><h2>Error trade-off explorer</h2><div className="mt-3 grid gap-3 md:grid-cols-3"><label className="label">Significance level<input type="range" min=".01" max=".1" step=".01" value={alpha} onChange={e=>setAlpha(Number(e.target.value))}/><span>{alpha.toFixed(2)}</span></label><label className="label">Illustrative sample size<input type="range" min="10" max="200" value={sample} onChange={e=>setSample(Number(e.target.value))}/><span>{sample}</span></label><label className="label">Illustrative effect size<input type="range" min=".1" max="3" step=".1" value={effect} onChange={e=>setEffect(Number(e.target.value))}/><span>{effect.toFixed(1)}</span></label></div><p className="mt-3">Type I error ceiling under the null: α={alpha.toFixed(2)}. Illustrative power indicator: {(illustrative*100).toFixed(0)}%; illustrative Type II indicator: {((1-illustrative)*100).toFixed(0)}%.</p><p className="muted mt-2 text-sm">These indicators demonstrate direction, not universal exact probabilities. Actual power depends on the test, distribution, variance, sampling design, and true parameter.</p></section>}
