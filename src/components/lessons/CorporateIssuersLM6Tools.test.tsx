import{describe,expect,it}from'vitest';import{calculateCostOfCapital}from'./CostOfCapitalBuilder';import{mmScenario}from'./CapitalStructureMMExplorer';
describe('CI-LM6 tools',()=>{it('calculates WACC and component contributions',()=>{const r=calculateCostOfCapital(.3,.05,.25,.1,.08,.6,.10);expect(r.wacc).toBeCloseTo(.07925,6);expect(r.weight).toBe(1)});it('applies the debt tax shield only to debt',()=>expect(calculateCostOfCapital(.4,.05,.25,0,0,.6,.10).debt).toBeCloseTo(.015));it('computes all four MM outputs',()=>{const r=mmScenario(100,40,.25,.10,.05,60);expect(r.noTaxValue).toBe(100);expect(r.taxValue).toBe(110);expect(r.noTaxEquityCost).toBeCloseTo(.133333,5);expect(r.taxEquityCost).toBeCloseTo(.125,5)})});
