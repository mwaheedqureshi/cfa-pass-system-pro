import {fireEvent,render,screen} from '@testing-library/react';import {describe,expect,it} from 'vitest';import {HypothesisTestDecisionTool} from './HypothesisTestDecisionTool';import {PValueExplorer} from './PValueExplorer';import {SimpleRegressionCalculator} from './SimpleRegressionCalculator';import {RegressionOutputInterpreter} from './RegressionOutputInterpreter';
describe('v1.5 learning tools',()=>{it('exposes accessible hypothesis controls and fail-to-reject wording',()=>{render(<HypothesisTestDecisionTool/>);expect(screen.getByLabelText('Test statistic')).toBeInTheDocument();fireEvent.change(screen.getByLabelText('Test statistic'),{target:{value:'0'}});expect(screen.getByText('Fail to reject the null hypothesis')).toBeInTheDocument()});it('validates degrees of freedom and renders p-value graphic',()=>{render(<PValueExplorer/>);fireEvent.change(screen.getByLabelText('Distribution'),{target:{value:'t'}});fireEvent.change(screen.getByLabelText('Degrees of freedom'),{target:{value:'0'}});expect(screen.getByRole('alert')).toHaveTextContent('positive integer');expect(screen.getByRole('img')).toHaveAccessibleName()});it('updates regression output and rejects malformed values',()=>{render(<SimpleRegressionCalculator/>);expect(screen.getByText(/SSE=0.7000/)).toBeInTheDocument();fireEvent.change(screen.getByLabelText('X observations, comma separated'),{target:{value:'1,bad'}});expect(screen.getByRole('alert')).toBeInTheDocument()});it('supports keyboard-operable interpretation buttons',()=>{render(<RegressionOutputInterpreter/>);const button=screen.getByRole('button',{name:'Predicted Y rises 0.8'});button.focus();fireEvent.keyDown(button,{key:'Enter'});fireEvent.click(button);expect(screen.getByRole('status')).toHaveTextContent('Correct')})});
