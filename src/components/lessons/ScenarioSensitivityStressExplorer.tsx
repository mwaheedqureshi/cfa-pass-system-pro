import {useState} from 'react';const methods={base:'Uses one central set of assumptions as a reference.',sensitivity:'Changes one input at a time to isolate its influence.',scenario:'Changes a coherent group of inputs together.',stress:'Examines an extreme but plausible adverse configuration.',simulation:'Draws many joint outcomes from specified probability assumptions.'};export function ScenarioSensitivityStressExplorer(){const[k,setK]=useState<keyof typeof methods>('base');return <section className="card"><h2>Scenario, sensitivity, and stress explorer</h2><label className="label mt-3">Analysis method<select value={k} onChange={e=>setK(e.target.value as keyof typeof methods)}>{Object.keys(methods).map(x=><option key={x}>{x}</option>)}</select></label><p role="status" className="mt-3">{methods[k]}</p></section>}
