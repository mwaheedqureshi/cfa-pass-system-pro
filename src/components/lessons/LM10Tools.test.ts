import{describe,expect,it}from'vitest';import{analyzeReportingFlags}from'./FinancialReportingRedFlagAnalyzer';import{accountingChoiceImpact}from'./AccountingBiasImpactExplorer';
describe('LM10 analytical tools',()=>{it('identifies coherent warning signals without alleging fraud',()=>{const r=analyzeReportingFlags({revenueGrowth:10,receivablesGrowth:28,cfo:60,netIncome:100,recurringAdjustments:true,estimateChange:false,qualitativeFlags:1});expect(r.cashConversion).toBe(.6);expect(r.flags).toHaveLength(4);expect(r.conclusion).toContain('not proof')});it('handles a zero earnings denominator',()=>expect(analyzeReportingFlags({revenueGrowth:5,receivablesGrowth:5,cfo:10,netIncome:0,recurringAdjustments:false,estimateChange:false,qualitativeFlags:0}).cashConversion).toBeNull());it('shows aggressive timing and reversal effects',()=>expect(accountingChoiceImpact('capitalize-cost',30)).toEqual({classification:'aggressive',effects:{earnings:30,assets:30,liabilities:0,cfo:0,futureEarnings:-30},note:'Illustrative timing effect: cash is unchanged at recognition and future reversal depends on subsequent events.'}));it('does not label conservatism as neutral',()=>{const r=accountingChoiceImpact('increase-reserve',12);expect(r.classification).toBe('conservative');expect(r.effects).toMatchObject({earnings:-12,liabilities:12,futureEarnings:12})})});
