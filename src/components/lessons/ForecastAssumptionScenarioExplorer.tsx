import {useState} from 'react';

export type ScenarioInputs={baseRevenue:number;priceChange:number;volumeChange:number;costInflation:number;passThrough:number;baseGrossMargin:number;competitionPressure:number;horizon:number};
export function evaluateScenario(x:ScenarioInputs){const priceEffect=x.priceChange+x.costInflation*x.passThrough/100-x.competitionPressure;const revenue=x.baseRevenue*(1+priceEffect/100)*(1+x.volumeChange/100);const baseCost=x.baseRevenue*(1-x.baseGrossMargin/100);const cost=baseCost*(1+x.costInflation/100)*(1+x.volumeChange/100);const grossProfit=revenue-cost;return{priceEffect,revenue,cost,grossProfit,grossMargin:revenue?grossProfit/revenue*100:0,cumulativeRevenue:revenue*x.horizon};}
const presets={base:{priceChange:1,volumeChange:3,costInflation:3,passThrough:70,competitionPressure:0},downside:{priceChange:0,volumeChange:-4,costInflation:6,passThrough:30,competitionPressure:2},upside:{priceChange:2,volumeChange:6,costInflation:2,passThrough:90,competitionPressure:0}} as const;
export function ForecastAssumptionScenarioExplorer(){const[x,setX]=useState<ScenarioInputs>({baseRevenue:1000,baseGrossMargin:40,horizon:5,...presets.base}),r=evaluateScenario(x);const field=(key:keyof ScenarioInputs,label:string)=><label className="label">{label}<input className="input mt-1" type="number" step="0.1" value={x[key]} onChange={e=>setX({...x,[key]:Number(e.target.value)})}/></label>;
 const scenario=(name:keyof typeof presets)=>setX({...x,...presets[name]});return <section className="card" aria-labelledby="scenario-title"><h3 id="scenario-title">Forecast Assumption and Scenario Explorer</h3><p className="muted mt-2">Translate price, volume, inflation, pass-through, competition, and horizon judgments into a deterministic operating forecast.</p><div className="mt-3 flex flex-wrap gap-2">{(['base','downside','upside'] as const).map(name=><button className="btn capitalize" key={name} onClick={()=>scenario(name)}>{name}</button>)}</div><div className="mt-4 grid gap-3 sm:grid-cols-2 lg:grid-cols-4">{field('baseRevenue','Base revenue')}{field('priceChange','Independent price change %')}{field('volumeChange','Volume change %')}{field('costInflation','Input-cost inflation %')}{field('passThrough','Cost pass-through %')}{field('competitionPressure','Competitive price pressure %')}{field('baseGrossMargin','Base gross margin %')}{field('horizon','Forecast horizon (years)')}</div><div className="mt-5 grid gap-3 sm:grid-cols-2 lg:grid-cols-4"><div><strong>Net price effect</strong><p>{r.priceEffect.toFixed(2)}%</p></div><div><strong>Forecast revenue</strong><p>{r.revenue.toFixed(1)}</p></div><div><strong>Gross margin</strong><p>{r.grossMargin.toFixed(2)}%</p></div><div><strong>Illustrative horizon revenue</strong><p>{r.cumulativeRevenue.toFixed(1)}</p></div></div><p className="muted mt-4">Bias prompt: identify evidence that would contradict this scenario, compare relevant base rates, and avoid interpreting a narrow deterministic result as certainty.</p></section>}
