import {useState} from 'react';const items=[['A backtest uses next-quarter earnings to form today’s signal.','Look-ahead bias'],['A fund study includes only products still operating.','Survivorship bias'],['Feature scaling is fit using the full dataset before splitting.','Data leakage'],['A model memorizes training noise and fails on new data.','Overfitting'],['An analyst follows an automated recommendation despite contradictory evidence.','Automation bias']];export function BiasLeakageScenarioTrainer(){const[i,setI]=useState(0),[answer,setAnswer]=useState('');return <section className="card"><h2>Bias and leakage scenario trainer</h2><label className="label mt-3">Scenario<select value={i} onChange={e=>{setI(Number(e.target.value));setAnswer('')}}>{items.map((_,j)=><option key={j} value={j}>Scenario {j+1}</option>)}</select></label><p className="mt-3">{items[i][0]}</p><label className="label mt-3">Classification<select value={answer} onChange={e=>setAnswer(e.target.value)}><option value="">Choose…</option>{['Look-ahead bias','Survivorship bias','Selection bias','Data leakage','Class imbalance','Overfitting','Automation bias'].map(x=><option key={x}>{x}</option>)}</select></label>{answer&&<p role="status">{answer===items[i][1]?'Correct. ':'Review the timing or selection mechanism. '}The key risk is {items[i][1]}.</p>}</section>}
