import{describe,expect,it}from'vitest';import{calculateCashFlowRatios,calculateFreeCashFlows}from'./FreeCashFlowCoverageCalculator';
describe('LM5 free cash flow tool',()=>{it('calculates FCFF and FCFE',()=>expect(calculateFreeCashFlows({cfo:80,interest:10,taxRate:.4,fixedCapitalInvestment:30,netBorrowing:5})).toEqual({fcff:56,fcfe:55}));it('calculates the official coverage ratios',()=>expect(calculateCashFlowRatios({cfo:120,revenue:600,totalDebt:400,interestPaid:10,taxesPaid:20,longTermAssetPurchases:80,debtRepayment:30,dividendsPaid:24,investingFinancingOutflows:150})).toEqual({cashFlowToRevenue:.2,debtCoverage:.3,interestCoverage:15,reinvestment:1.5,debtPayment:4,dividendPayment:5,investingFinancing:.8}))});
