import {fireEvent,render,screen} from '@testing-library/react';import {describe,expect,it} from 'vitest';import {DistributionShapeExplorer} from './DistributionShapeExplorer';describe('distribution shape explorer',()=>{it('maps controls to accessible shape explanations',()=>{render(<DistributionShapeExplorer/>);expect(screen.getByRole('img').getAttribute('aria-label')).toContain('symmetric');fireEvent.click(screen.getByRole('button',{name:'positive'}));expect(screen.getByRole('img').getAttribute('aria-label')).toContain('mode < median < mean');fireEvent.click(screen.getByRole('button',{name:'leptokurtic'}));expect(screen.getByRole('img').getAttribute('aria-label')).toContain('heavier tails')})});
