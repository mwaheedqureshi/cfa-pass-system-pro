import{describe,expect,it}from'vitest';import{classifyDeferredTax}from'./DeferredTaxExplorer';import{reconcileTaxRate,taxRates}from'./TaxRateReconciliationAnalyzer';
describe('LM9 interactive calculations',()=>{it('classifies asset temporary differences',()=>{expect(classifyDeferredTax('asset',1200,900,.25)).toEqual({difference:300,classification:'DTL',amount:75});expect(classifyDeferredTax('asset',700,900,.25).classification).toBe('DTA')});it('reverses direction for liabilities',()=>{expect(classifyDeferredTax('liability',700,900,.25).classification).toBe('DTL');expect(classifyDeferredTax('liability',1200,900,.25).classification).toBe('DTA')});it('calculates effective and cash rates',()=>expect(taxRates(1000,230,180)).toEqual({effective:.23,cash:.18}));it('reconciles rate effects',()=>expect(reconcileTaxRate(.25,[.02,-.04])).toBeCloseTo(.23,10))});
