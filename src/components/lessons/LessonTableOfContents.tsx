import type {headingsFromMarkdown} from '../../services/contentService'; export function LessonTableOfContents({headings}:{headings:ReturnType<typeof headingsFromMarkdown>}){return <nav aria-label="Lesson table of contents" className="card sticky top-20 hidden max-h-[calc(100vh-6rem)] overflow-auto lg:block"><strong>On this page</strong><ol className="mt-3 space-y-2 text-sm">{headings.map(h=><li key={h.id} className={h.level===3?'pl-3':''}><a href={`#${h.id}`} className="text-slate-600 hover:text-blue-600 dark:text-slate-300">{h.text}</a></li>)}</ol></nav>}
