import{useState}from'react';
export const calculateWorkingCapitalCycle=(dio:number,dso:number,dpo:number)=>({operatingCycle:dio+dso,cashConversionCycle:dio+dso-dpo});
export default function WorkingCapitalCycleTrainer(){const[dio,setDio]=useState(40),[dso,setDso]=useState(30),[dpo,setDpo]=useState(25);const r=calculateWorkingCapitalCycle(dio,dso,dpo);return <section className="card p-5"><h3 className="text-lg font-semibold">Working Capital Cycle Trainer</h3><p className="mt-1 text-sm text-slate-600">Change the cycle inputs to distinguish the operating cycle from the cash conversion cycle.</p><div className="mt-4 grid gap-3 sm:grid-cols-3"><label className="text-sm">DIO (days)<input aria-label="DIO" className="input mt-1" type="number" value={dio} onChange={e=>setDio(Number(e.target.value))}/></label><label className="text-sm">DSO (days)<input aria-label="DSO" className="input mt-1" type="number" value={dso} onChange={e=>setDso(Number(e.target.value))}/></label><label className="text-sm">DPO (days)<input aria-label="DPO" className="input mt-1" type="number" value={dpo} onChange={e=>setDpo(Number(e.target.value))}/></label></div><div className="mt-4 grid gap-3 sm:grid-cols-2"><div className="rounded-lg bg-slate-50 p-3"><strong>Operating cycle: {r.operatingCycle} days</strong><p className="text-sm">DIO + DSO. Supplier timing is excluded.</p></div><div className="rounded-lg bg-blue-50 p-3"><strong>CCC: {r.cashConversionCycle} days</strong><p className="text-sm">Operating cycle − DPO. Higher DIO/DSO lengthens it; higher DPO shortens it.</p></div></div></section>}
