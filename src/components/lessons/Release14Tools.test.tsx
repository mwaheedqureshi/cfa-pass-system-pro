import {fireEvent,render,screen} from '@testing-library/react';import {describe,expect,it} from 'vitest';import {BinomialProbabilityCalculator} from './BinomialProbabilityCalculator';import {ConfidenceIntervalCalculator} from './ConfidenceIntervalCalculator';import {SamplingMethodExplorer} from './SamplingMethodExplorer';import {ZScoreProbabilityCalculator} from './ZScoreProbabilityCalculator';describe('v1.4 lesson tools',()=>{it('exposes labeled Z inputs and validation',()=>{render(<ZScoreProbabilityCalculator/>);expect(screen.getByLabelText('Observation')).toBeInTheDocument();fireEvent.change(screen.getByLabelText('Standard deviation'),{target:{value:'0'}});expect(screen.getByRole('alert')).toHaveTextContent('greater than zero')});it('updates binomial output',()=>{render(<BinomialProbabilityCalculator/>);expect(screen.getByText(/P\(X=x\)=/)).toBeInTheDocument();fireEvent.change(screen.getByLabelText('Trials n'),{target:{value:'2.5'}});expect(screen.getByRole('alert')).toHaveTextContent('integer')});it('shows confidence output and sampling feedback',()=>{render(<ConfidenceIntervalCalculator/>);expect(screen.getByText('Critical value')).toBeInTheDocument();render(<SamplingMethodExplorer/>);fireEvent.click(screen.getByRole('button',{name:'simple random'}));expect(screen.getByText(/Correct/)).toBeInTheDocument()})});
