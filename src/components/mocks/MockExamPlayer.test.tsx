import {fireEvent,render,screen} from '@testing-library/react';import {MemoryRouter} from 'react-router-dom';import {describe,expect,it,vi} from 'vitest';import {MockExamPlayer} from './MockExamPlayer';import {newAttempt} from '../../mocks/mockService';import type {MockExam} from '../../mocks/types';
const mock:MockExam={id:'m',title:'Mock',provider:'Local',year:2025,sourceFile:'x',sourceFolder:'x',questionCount:1,timeLimitMinutes:1,questions:[{id:'q',mockId:'m',sourceQuestionNumber:1,topic:'Unmapped',stem:'Choose one',choices:['Wrong','Right','Other','Fourth'],correctChoiceIndex:1,explanation:'Source explanation',extractionConfidence:'high',needsManualReview:false,mappingConfidence:'unmapped',relatedFormulaIds:[]}]};
describe('mock exam player',()=>{it('hides answers in exam mode and preserves four source choices',()=>{render(<MemoryRouter><MockExamPlayer mock={mock} initial={newAttempt(mock,'exam')} onSave={vi.fn()} onFinish={vi.fn()}/></MemoryRouter>);expect(screen.getAllByRole('button',{name:/^(A|B|C|D)\./})).toHaveLength(4);fireEvent.click(screen.getByRole('button',{name:/B\.Right/}));expect(screen.queryByText('Source explanation')).not.toBeInTheDocument()});it('shows immediate feedback in practice mode',()=>{render(<MemoryRouter><MockExamPlayer mock={mock} initial={newAttempt(mock,'practice')} onSave={vi.fn()} onFinish={vi.fn()}/></MemoryRouter>);fireEvent.click(screen.getByRole('button',{name:/A\.Wrong/}));expect(screen.getByText('Incorrect')).toBeInTheDocument();expect(screen.getByText('Source explanation')).toBeInTheDocument()});it('supports navigation, flags, pause, and early-submit confirmation',()=>{render(<MemoryRouter><MockExamPlayer mock={mock} initial={newAttempt(mock,'exam')} onSave={vi.fn()} onFinish={vi.fn()}/></MemoryRouter>);fireEvent.click(screen.getByRole('button',{name:'Flag for review'}));fireEvent.click(screen.getByRole('button',{name:'Pause'}));expect(screen.getByText(/Exam paused/)).toBeInTheDocument();fireEvent.click(screen.getByRole('button',{name:'Resume'}));fireEvent.click(screen.getByRole('button',{name:'Finish exam'}));expect(screen.getByRole('dialog')).toHaveTextContent('unanswered')})});
