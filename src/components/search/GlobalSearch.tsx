
import {Search} from 'lucide-react'; import {useState} from 'react'; import {useSearch} from '../../hooks/useSearch'; import {SearchResults} from './SearchResults'; export function GlobalSearch(){const[q,setQ]=useState('');const results=useSearch(q);return <div className="relative"><label className="sr-only" htmlFor="global-search">Search study content</label><Search className="absolute left-3 top-2.5 text-slate-400" size={18}/><input id="global-search" value={q} onChange={e=>setQ(e.target.value)} className="w-full rounded-lg border-0 bg-white/10 py-2 pl-10 pr-3 text-sm text-white placeholder:text-slate-300 focus:ring-2 focus:ring-blue-400" placeholder="Search lessons, formulas, flashcardsâ€¦"/>{q.trim()&&<SearchResults results={results} query={q} onChoose={()=>setQ('')}/>}</div>}
