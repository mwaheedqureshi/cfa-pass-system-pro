import {Link} from 'react-router-dom'; import type {SearchItem} from '../../services/searchService'; export function SearchResults({results,onChoose}:{results:SearchItem[];onChoose:()=>void}){return <div className="absolute left-0 right-0 top-11 max-h-80 overflow-y-auto rounded-lg border bg-white p-2 text-slate-900 shadow-xl dark:border-slate-700 dark:bg-slate-800 dark:text-white">{results.length?results.slice(0,12).map((r,i)=><Link key={`${r.category}-${i}`} to={r.to} onClick={onChoose} className="block rounded-md p-2 hover:bg-slate-100 dark:hover:bg-slate-700"><span className="text-xs font-semibold uppercase text-blue-600">{r.category}</span><strong className="block text-sm">{r.title}</strong><span className="line-clamp-1 text-xs text-slate-500 dark:text-slate-300">{r.excerpt}</span></Link>):<p className="p-3 text-sm text-slate-500">No matching study content.</p>}</div>}
