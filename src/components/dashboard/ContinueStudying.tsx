import {ArrowRight,BookOpen} from 'lucide-react';import {Link} from 'react-router-dom';import {lessons} from '../../content/lessonManifest';import {useProgress} from '../../hooks/useProgress';export function ContinueStudying(){const{progress}=useProgress();const lesson=lessons.find(l=>l.id===progress.lastOpenedLesson)??lessons[0];return <section className="card"><BookOpen className="text-blue-600"/><h2 className="mt-3">Continue studying</h2><p className="muted mt-2">{lesson.title}</p><Link className="btn btn-primary mt-5" to={`/lessons/${lesson.id}`}>Open lesson <ArrowRight size={16}/></Link></section>}
