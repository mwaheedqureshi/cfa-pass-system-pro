import {topics} from '../../content/curriculum'; import {lessons} from '../../content/lessonManifest'; import {useProgress} from '../../hooks/useProgress'; export function TopicProgress(){const{progress}=useProgress();return <section className="card"><h2>Topic progress</h2><div className="mt-4 space-y-3">{topics.map(t=>{const topicLessons=lessons.filter(l=>l.topicId===t.id);const done=topicLessons.filter(l=>progress.completedLessons.includes(l.id)).length;return <div key={t.id} className="flex items-center justify-between gap-2 text-sm"><span>{t.title}</span><span className={t.status==='pending'?'text-slate-400':'font-semibold text-blue-600'}>{t.status==='pending'?'Content pending':`${done} / ${topicLessons.length}`}</span></div>})}</div></section>}
