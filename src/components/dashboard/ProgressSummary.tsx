import {BookCheck,CheckCircle2,Target} from 'lucide-react'; import {useProgress} from '../../hooks/useProgress'; import {completionPercent,quizScore} from '../../services/progressService'; import {lessons} from '../../content/lessonManifest'; import {ProgressBar} from '../common/ProgressBar'; export function ProgressSummary(){const{progress}=useProgress();const values=[[`${completionPercent(progress.completedLessons,lessons.length)}%`,'Overall progress',Target],[`${quizScore(progress.quizAttempts)}%`,'Quiz average',CheckCircle2],[String(progress.completedLessons.length),'Lessons completed',BookCheck],[String(progress.quizAttempts.length),'Questions attempted',CheckCircle2]] as const;return <><div className="stats-grid">{values.map(([value,label,Icon])=><div className="stat-card" key={label}><Icon/><strong>{value}</strong><span>{label}</span></div>)}</div><div className="card mt-5"><ProgressBar value={completionPercent(progress.completedLessons,lessons.length)} label="Available lesson completion"/></div></>}
