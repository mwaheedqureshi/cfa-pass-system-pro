import {Link} from 'react-router-dom';import {lessons} from '../../content/lessonManifest';import {useProgress} from '../../hooks/useProgress';
export function LessonHistory(){const{progress}=useProgress();const viewed=progress.recentlyViewedLessons.map(id=>lessons.find(l=>l.id===id)).filter(Boolean);const completed=lessons.find(l=>l.id===progress.lastCompletedLesson);return <section className="card"><h2>Lesson history</h2>{completed?<p className="mt-3 text-sm">Last completed: <Link className="font-semibold text-blue-700 underline dark:text-blue-300" to={`/lessons/${completed.id}`}>{completed.title}</Link></p>:<p className="muted mt-3 text-sm">No completed lesson yet.</p>}<h3 className="mt-4 text-base">Recently viewed</h3>{viewed.length?<ul className="mt-2 space-y-2">{viewed.map(lesson=><li key={lesson!.id}><Link className="text-sm text-blue-700 underline dark:text-blue-300" to={`/lessons/${lesson!.id}`}>{lesson!.title}</Link></li>)}</ul>:<p className="muted mt-2 text-sm">Open a lesson to build your history.</p>}</section>}
