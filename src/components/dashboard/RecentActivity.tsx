import {useProgress} from '../../hooks/useProgress'; export function RecentActivity(){const{progress}=useProgress();return <section className="card"><h2>Recent activity</h2>{progress.recentActivity.length?<ul className="mt-4 space-y-3">{progress.recentActivity.slice(0,5).map(a=><li key={a.id} className="border-b pb-2 text-sm dark:border-slate-700"><span>{a.label}</span><time className="block text-xs text-slate-500">{new Date(a.timestamp).toLocaleString()}</time></li>)}</ul>:<p className="muted mt-3">Your lesson, quiz, and flashcard activity will appear here.</p>}</section>}
