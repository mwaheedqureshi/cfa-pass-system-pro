import {BarChart3,BookOpen,Calculator,ChartNoAxesCombined,FileQuestion,LayoutDashboard,Lock,Layers3} from 'lucide-react'; import {NavLink} from 'react-router-dom'; import {topics} from '../../content/curriculum';
const nav=[['/','Dashboard',LayoutDashboard],['/topics','Study Topics',BookOpen],['/practice','Practice',FileQuestion],['/formulas','Formula Explorer',Calculator],['/flashcards','Flashcards',Layers3],['/progress','Progress',BarChart3]] as const;
export function AppSidebar({open,onClose}:{open:boolean;onClose:()=>void}){return <><button className={`fixed inset-0 z-20 bg-black/40 md:hidden ${open?'block':'hidden'}`} onClick={onClose} aria-label="Close navigation"/><aside className={`fixed bottom-0 left-0 top-16 z-30 w-72 overflow-y-auto border-r bg-white p-4 transition-transform dark:border-slate-700 dark:bg-slate-900 md:sticky md:translate-x-0 ${open?'translate-x-0':'-translate-x-full'}`}><nav aria-label="Primary" className="space-y-1">{nav.map(([to,label,Icon])=><NavLink key={to} to={to} end={to==='/'} onClick={onClose} className={({isActive})=>`nav-link ${isActive?'active':''}`}><Icon size={18}/>{label}</NavLink>)}</nav><div className="my-5 border-t dark:border-slate-700"/><p className="px-3 text-xs font-semibold uppercase tracking-wider text-slate-500">Level I topics</p><nav className="mt-2 space-y-1" aria-label="Topics">{topics.map(t=><NavLink key={t.id} to={`/topics/${t.id}`} onClick={onClose} className="nav-link text-sm">{t.status==='pending'?<Lock size={14}/>:<ChartNoAxesCombined size={14}/>}<span className="flex-1">{t.title}</span>{t.status==='pending'&&<span className="text-xs text-slate-400">Pending</span>}</NavLink>)}</nav><p className="mt-6 px-3 text-xs text-slate-500">Independent study tool. Not affiliated with or endorsed by CFA Institute.</p></aside></>}
