import {BookOpen,FileQuestion,Home,Layers3,LineChart} from 'lucide-react'; import {NavLink} from 'react-router-dom'; const links=[['/',Home,'Home'],['/topics',BookOpen,'Topics'],['/practice',FileQuestion,'Practice'],['/flashcards',Layers3,'Cards'],['/progress',LineChart,'Progress']] as const; export function MobileNavigation(){return <nav aria-label="Mobile navigation" className="fixed inset-x-0 bottom-0 z-40 flex justify-around border-t bg-white px-2 py-2 dark:border-slate-700 dark:bg-slate-900 md:hidden">{links.map(([to,Icon,label])=><NavLink key={to} to={to} className={({isActive})=>`flex flex-col items-center gap-1 rounded px-3 py-1 text-xs ${isActive?'text-blue-600':'text-slate-500'}`}><Icon size={19}/>{label}</NavLink>)}</nav>}
