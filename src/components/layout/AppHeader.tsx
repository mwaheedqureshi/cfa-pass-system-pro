import {Menu,Moon,Search,Sun,X} from 'lucide-react'; import {useState} from 'react'; import {useNavigate} from 'react-router-dom'; import {useTheme} from '../../hooks/useTheme'; import {GlobalSearch} from '../search/GlobalSearch';
export function AppHeader({onMenu}:{onMenu:()=>void}){const{theme,toggle}=useTheme();const[search,setSearch]=useState(false);const nav=useNavigate();return <header className="sticky top-0 z-40 flex h-16 items-center gap-3 bg-navy-900 px-4 text-white shadow-lg"><button className="icon-btn md:hidden" onClick={onMenu} aria-label="Open navigation"><Menu/></button><button className="min-w-0 text-left" onClick={()=>nav('/')}><strong className="block truncate tracking-wide">CFA PASS SYSTEM</strong><span className="block text-xs text-blue-200">Professional Edition</span></button><div className="ml-auto hidden w-full max-w-md md:block"><GlobalSearch/></div><button className="icon-btn md:hidden" onClick={()=>setSearch(v=>!v)} aria-label="Toggle search">{search?<X/>:<Search/>}</button><button className="icon-btn" onClick={toggle} aria-label={`Use ${theme==='dark'?'light':'dark'} theme`}>{theme==='dark'?<Sun/>:<Moon/>}</button>{search&&<div className="absolute inset-x-0 top-16 bg-navy-900 p-3 md:hidden"><GlobalSearch/></div>}</header>}
