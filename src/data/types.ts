export type Topic={id:string;title:string;description:string;lessonCount:number;status:'available'|'pending'};
// Modules 9+ may use independently authored curriculum-mapping scope statements when authorized official outcome wording is unavailable.
export type CurriculumClassification={officialModuleId:string;studyLessonId:string;subdivision?:'A'|'B';supplementary?:boolean;legacySourceLessonId?:string};
export type Lesson={id:string;topicId:string;title:string;filePath:string;officialModuleTitle:string;officialModuleId?:string;subdivision?:'A'|'B';supplementary?:boolean;estimatedMinutes:number;difficulty:'Foundation'|'Intermediate'|'Advanced';order:number;status:'available'|'pending';questionSetId:string;flashcardSetId:string;formulaIds:string[];officialLearningOutcomes?:string[];scopeStatements?:string[]};
export type Question={id:string;lessonId:string;studyLessonId?:string;topicId:string;officialModuleId?:string;subdivision?:'A'|'B';supplementary?:boolean;legacySourceLessonId?:string;officialLearningOutcome?:string;scopeStatement?:string;difficulty:'easy'|'medium'|'hard';estimatedSeconds:number;stem:string;choices:[string,string,string];correctChoiceIndex:number;explanation:string;incorrectChoiceExplanations:[string,string,string];relatedFormulaIds:string[];tags:string[]};
export type Formula={id:string;name:string;expression:string;meaning:string;variables:Record<string,string>;intuition?:string;workedExample:string;commonMistake:string;relatedLessonId:string;studyLessonId?:string;officialModuleId?:string;subdivision?:'A'|'B';supplementary?:boolean;legacySourceLessonId?:string;relatedLearningOutcome?:string;scopeStatement?:string;tags?:string[]};
export type Flashcard={id:string;lessonId:string;studyLessonId?:string;officialModuleId?:string;subdivision?:'A'|'B';supplementary?:boolean;legacySourceLessonId?:string;front:string;back:string;tags:string[]};
export type Confidence='confident'|'unsure'|'guess'; export type FlashcardRating='again'|'hard'|'good';
export type QuizAttempt={questionId:string;lessonId:string;correct:boolean;selectedIndex:number;confidence:Confidence;answeredAt:string;curriculumVersion?:string;legacyOfficialModuleId?:string};
export type Activity={id:string;type:'lesson'|'quiz'|'flashcard'|'note';label:string;timestamp:string};
export type ProgressState={version:2;curriculumMigration:'2027-quant-v1';completedLessons:string[];bookmarkedLessons:string[];notes:Record<string,string>;legacyNotes:Record<string,string>;quizAttempts:QuizAttempt[];flashcardReviews:Record<string,{rating:FlashcardRating;reviewedAt:string}>;lastOpenedLesson:string|null;recentlyViewedLessons:string[];lastCompletedLesson:string|null;examDate:string;dailyQuestionGoal:number;theme:'light'|'dark';recentActivity:Activity[]};
