import {useState} from 'react'; import {AppHeader} from '../components/layout/AppHeader'; import {AppSidebar} from '../components/layout/AppSidebar'; import {MobileNavigation} from '../components/layout/MobileNavigation'; import {PageContainer} from '../components/layout/PageContainer'; import {ProgressProvider} from '../hooks/useProgress'; import {AppRouter} from './router'; export function App(){const[menu,setMenu]=useState(false);return <ProgressProvider><a href="#main-content" className="skip-link">Skip to content</a><AppHeader onMenu={()=>setMenu(true)}/><div className="flex min-h-[calc(100vh-4rem)]"><AppSidebar open={menu} onClose={()=>setMenu(false)}/><PageContainer><AppRouter/><footer className="mt-12 border-t pt-5 text-xs text-slate-500 dark:border-slate-700">CFA PASS SYSTEM is an independent educational application and is not affiliated with or endorsed by CFA Institute. CFA® is a registered trademark owned by CFA Institute.</footer></PageContainer></div><MobileNavigation/></ProgressProvider>}
