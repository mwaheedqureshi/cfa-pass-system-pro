import {describe,expect,it} from 'vitest'; import {lessons} from './lessonManifest'; describe('lesson manifest',()=>{it('has unique ids and complete required fields',()=>{expect(new Set(lessons.map(x=>x.id)).size).toBe(lessons.length);for(const l of lessons){expect(l.filePath.endsWith('.md')).toBe(true);expect(l.officialLearningOutcomes.length).toBeGreaterThan(0);expect(l.formulaIds.length).toBeGreaterThan(0)}})});
