import {useMemo} from 'react'; import {lessons} from '../content/lessonManifest'; import {topics} from '../content/curriculum'; import formulasData from '../data/formulas/quantitative.json'; import cardsData from '../data/flashcards/quantitative.json'; import type {Flashcard,Formula} from '../data/types'; import {buildSearchIndex,matchSearch} from '../services/searchService'; const formulas=formulasData as unknown as Formula[];const cards=cardsData as Flashcard[];export function useSearch(query:string,markdown=''){const index=useMemo(()=>buildSearchIndex(topics,lessons,formulas,cards,markdown),[markdown]);return matchSearch(index,query)}
