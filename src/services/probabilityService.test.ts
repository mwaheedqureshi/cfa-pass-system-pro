import {describe,expect,it} from 'vitest';import {probabilityTree,twoAssetPortfolio} from './probabilityService';describe('probability utilities',()=>{it('calculates branches, total probability, and Bayes posterior',()=>{const r=probabilityTree({priorA:.4,successGivenA:.7,successGivenNotA:.2});expect('error'in r).toBe(false);if(!('error'in r)){expect(r.jointASuccess).toBeCloseTo(.28);expect(r.jointNotASuccess).toBeCloseTo(.12);expect(r.totalSuccess).toBeCloseTo(.4);expect(r.posteriorA).toBeCloseTo(.7)}});it('rejects invalid probability',()=>expect(probabilityTree({priorA:1.2,successGivenA:.5,successGivenNotA:.2}).error).toBeTruthy());it('calculates expected return, variance, and standard deviation',()=>{const r=twoAssetPortfolio({weightA:.6,weightB:.4,returnA:.08,returnB:.04,standardDeviationA:.15,standardDeviationB:.08,correlation:.25});expect('error'in r).toBe(false);if(!('error'in r)){expect(r.expectedReturn).toBeCloseTo(.064);expect(r.variance).toBeCloseTo(.010564);expect(r.standardDeviation).toBeCloseTo(Math.sqrt(.010564))}});it('validates weights and correlation',()=>{expect(twoAssetPortfolio({weightA:.7,weightB:.4,returnA:0,returnB:0,standardDeviationA:.1,standardDeviationB:.1,correlation:0}).error).toBeTruthy();expect(twoAssetPortfolio({weightA:.6,weightB:.4,returnA:0,returnB:0,standardDeviationA:.1,standardDeviationB:.1,correlation:2}).error).toBeTruthy()})});
