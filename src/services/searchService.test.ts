import {describe,expect,it} from 'vitest';import type {Flashcard,Formula,Lesson,Question,Topic} from '../data/types';import {buildSearchIndex,matchSearch,type SearchItem} from './searchService';const items:SearchItem[]=[{category:'Formulas',title:'Exact real return',excerpt:'Fisher',to:'/formulas',searchText:'nominal inflation purchasing power'},{category:'Lessons',title:'Holding-period return',excerpt:'Lesson',to:'/lesson',searchText:'income price'}];describe('search matching',()=>{it('matches normalized terms across fields',()=>{expect(matchSearch(items,'REAL inflation')).toHaveLength(1);expect(matchSearch(items,'price income')[0].title).toContain('Holding')});it('does not return everything for blank query',()=>expect(matchSearch(items,'  ')).toEqual([]));it('indexes question metadata for new modules',()=>{const topic:Topic={id:'quantitative-methods',title:'Quantitative Methods',description:'Statistics and probability',lessonCount:1,status:'available'};const lesson:Lesson={id:'quant-probability-06',topicId:topic.id,title:'Probability',filePath:'probability.md',officialModuleTitle:'Probability Concepts',estimatedMinutes:1,difficulty:'Intermediate',order:6,status:'available',questionSetId:'q',flashcardSetId:'f',formulaIds:['bayes-rule-06'],officialLearningOutcomes:['Apply Bayes']};const formula:Formula={id:'bayes-rule-06',name:'Bayes formula',expression:'posterior',meaning:'Updates probability',variables:{},workedExample:'Example',commonMistake:'Reverse direction',relatedLessonId:lesson.id};const card:Flashcard={id:'card',lessonId:lesson.id,front:'Bayes denominator?',back:'Total evidence probability',tags:['bayes']};const question:Question={id:'question',lessonId:lesson.id,topicId:topic.id,officialLearningOutcome:'Apply Bayes',difficulty:'easy',estimatedSeconds:30,stem:'Which denominator is used in Bayes formula?',choices:['Prior','Evidence','Likelihood'],correctChoiceIndex:1,explanation:'Use total evidence.',incorrectChoiceExplanations:['Prior is numerator input.','Correct.','Likelihood is numerator input.'],relatedFormulaIds:['bayes-rule-06'],tags:['posterior']};const index=buildSearchIndex([topic],[lesson],[formula],[card],'probability tree',[question]);expect(matchSearch(index,'posterior').some(x=>x.category==='Questions')).toBe(true);expect(matchSearch(index,'probability tree').some(x=>x.category==='Lessons')).toBe(true)})});
