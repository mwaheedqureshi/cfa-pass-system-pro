import {describe,expect,it} from 'vitest';import {capitalAllocation,covarianceFromCorrelation,meanVarianceUtility,minimumVarianceWeight,optimalRiskyAllocation,twoAssetPortfolio} from './portfolioService';
describe('portfolio calculations',()=>{
 it('calculates two-asset return, variance, and risk',()=>{const x=twoAssetPortfolio({weightA:.4,returnA:.05,returnB:.09,riskA:.10,riskB:.15,correlation:.25});expect(x.expectedReturn).toBeCloseTo(.074);expect(x.variance).toBeCloseTo(.0115);expect(x.risk).toBeCloseTo(Math.sqrt(.0115))});
 it('shows diversification and verifies correlation boundaries',()=>{const low=twoAssetPortfolio({weightA:.5,returnA:.06,returnB:.06,riskA:.1,riskB:.1,correlation:0}),high=twoAssetPortfolio({weightA:.5,returnA:.06,returnB:.06,riskA:.1,riskB:.1,correlation:1});expect(low.risk).toBeLessThan(high.risk);expect(twoAssetPortfolio({weightA:.6,returnA:0,returnB:0,riskA:.1,riskB:.15,correlation:-1}).risk).toBeCloseTo(0)});
 it('converts covariance and calculates minimum variance',()=>{expect(covarianceFromCorrelation(.12,.2,.25)).toBeCloseTo(.006);expect(minimumVarianceWeight(.1,.2,0)).toBeCloseTo(.8)});
 it('calculates lending, leverage, short-risky positions, and optimal allocation',()=>{expect(capitalAllocation(.75,.03,.09,.12)).toEqual({expectedReturn:.075,risk:.09});expect(capitalAllocation(1.25,.03,.09,.12)).toEqual({expectedReturn:.105,risk:.15});expect(capitalAllocation(-.5,.03,.09,.12).risk).toBe(.06);expect(optimalRiskyAllocation(.03,.09,.15,3)).toBeCloseTo(.8888889)});
 it('ranks utility and rejects invalid or nonfinite inputs',()=>{expect(meanVarianceUtility(.08,.1,4)).toBeCloseTo(.06);expect(()=>twoAssetPortfolio({weightA:2,returnA:0,returnB:0,riskA:0,riskB:0,correlation:0})).toThrow();expect(()=>covarianceFromCorrelation(.1,.2,1.1)).toThrow();expect(()=>optimalRiskyAllocation(.03,.09,0,3)).toThrow();expect(()=>meanVarianceUtility(Number.NaN,.1,3)).toThrow()});
});
