import {describe,expect,it} from 'vitest';import {describe as stats,parseObservations} from './statisticsService';describe('descriptive statistics',()=>{it('calculates population and sample measures',()=>{const r=stats([2,4,6]);expect(r.mean).toBe(4);expect(r.populationVariance).toBeCloseTo(8/3);expect(r.sampleVariance).toBe(4);expect(r.populationStandardDeviation).toBeCloseTo(Math.sqrt(8/3));expect(r.sampleStandardDeviation).toBe(2)});it('calculates odd and even medians',()=>{expect(stats([1,3,9]).median).toBe(3);expect(stats([2,4,8,10]).median).toBe(6)});it('reports malformed tokens',()=>expect(parseObservations('1, two, 3').error).toContain('two'));it('requires observations',()=>expect(parseObservations(' ').error).toBeTruthy())});
