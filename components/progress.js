const KEY="cfaPassProProgress";export function loadProgress(){try{return JSON.parse(localStorage.getItem(KEY))||{completedLessons:[],quizScores:{},lastLessonId:null,theme:"light"}}catch{return{completedLessons:[],quizScores:{},lastLessonId:null,theme:"light"}}}export function saveProgress(p){localStorage.setItem(KEY,JSON.stringify(p))}export function markLessonComplete(p,id){if(!p.completedLessons.includes(id))p.completedLessons.push(id);p.lastLessonId=id;saveProgress(p)}export function saveQuizScore(p,id,score,total){p.quizScores[id]={score,total,date:new Date().toISOString()};saveProgress(p)}